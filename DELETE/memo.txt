Aggiunte:

- retention (a scelta) (solo upload)

- aggiunta password da salvare invece di key (tutti)

Advance Settings

- steganografia (nascondere il file cifrato dentro immagini, tutto in browser) (carica una a scelta o una preimpostata) (tutti);​

- plausible deniability (due password, volume finto + volume vero) (tutti);

- eredità digitale (dead man’s switch) che libera la chiave a un contatto se smetti di “pingare” il sito (tutti o solo upload?); ​

- estensione .txt/.bin (tutti)

- scarica senza decriptare (solo upload)


- tasto copy
- sanitizeFilename
- keyToDisplay, setKeyToDisplay
- autoDecrypt, setAutoDecrypt


- github + buy me coffee + messaggio per info, consigli o altro in basso a sinistra

- faq


- We do not store IP addresses or user activity. Minimal connection logs may be temporarily retained by our infrastructure providers (Vercel/Supabase) solely for security and DDoS protection.

- Using Dropbox requires logging in. Dropbox will know you uploaded a file, but they cannot read its content (Zero Knowledge).

------

- modalità “BYO storage”: cifri in locale e poi connetti Google Drive/Dropbox dell’utente via API e carichi lì in forma cifrata
    Dropbox
    🖕 Google